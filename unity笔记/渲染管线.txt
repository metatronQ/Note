图形数据在GPU上经过运算处理，最后输出到屏幕的过程；
（CPU）游戏-》图形Api-》

（Draw Call：每帧调用显卡渲染物体的次数）

（GPU）顶点处理（找顶点）-》图元装配（连点成三角面）-》光栅化（计算三角面上的像素）-》像素处理（对像素区域进行处理，写入缓存）-》缓存

缓存：储存像素数据的内存块
	帧缓存：储存每个像素的色彩，即渲染后的图像，存于显卡中供其读取和输入；
	深度缓存 z-buffer：储存像素的深度信息，即物体到摄像机的距离，光栅化时进行计算或替换；