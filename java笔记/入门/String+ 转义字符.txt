char描述单一字符 ''

String描述字符串 ""，不是基本数据类型

对于打印的+号：
对于数学来说是相加，对于String来说是连接两个字符串（包括单一字符和数字，不是做加法运算：" "+1+2= 12，即所有类型->String)；
要想在和String“+”时能够进行运算，只需添加括号改变相应的优先级：加减乘除都一样，只要按照优先度是两个数据类型相运算，不与String类型运算即可：[" "+(1+1)-> 2]，[" "+ 1*1-> 1];
转义字符：一般print引号中用
换行（\n）
制表符（\t）
双引号（\"）、单引号（\'）
\（\\）